

function testFormula() {
    var myFormula = new chemicalFormula();
    myFormula.showElementMap();
    myFormula.addElement("H",1);
    myFormula.showElementMap();
    myFormula.addElement("I",2);
    myFormula.showElementMap();
}